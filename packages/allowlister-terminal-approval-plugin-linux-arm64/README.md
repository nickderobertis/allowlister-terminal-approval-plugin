# @nickderobertis/allowlister-terminal-approval-plugin-linux-arm64

The linux arm64 native binary for [@nickderobertis/allowlister-terminal-approval-plugin](https://www.npmjs.com/package/@nickderobertis/allowlister-terminal-approval-plugin).

You do not install this package directly — the parent package declares it as an
optional dependency and npm installs only the one matching your host.
